# 汤圆启动器 Rev.Ini 编辑器 — 完整设计规范

> 版本: v2 设计规范(推翻 v1 天蓝主题,2026-08-08 定稿;**2026-08-30 v2.3.1: 收编浅色主题+v2.3.0 新功能 UI;二轮按用户反馈改 Win11 圆角、直连/练枪重组**)
> 主题: **矢车菊蓝 #6495ED 纯色体系**(无渐变、无发光效果;由早期紫色渐变方案更换而来,v1.2 去除所有发光并换 SVG 图标;**深/浅双主题**,浅色令牌见 tokens.md §1.8)
> 来源: SuperDesign 产出,用户认可
> 唯一事实源: `docs/preview v1.html`(HTML 是唯一事实源;本文档随之维护,组件/令牌细节见 `design-system.md` / `tokens.md` / `rules.md`)
> 旧 `design/` 文档已删除 (本 docs/ 为唯一文档体系)

---

## 产品概述

CS:GO nosteam 玩家专用 Windows 桌面启动器,两个核心界面:

- **主页启动台** (360×510): 快速启动 & 玩家信息展示;服务器状态胶囊悬停展开分服面板, 练枪在标题栏
- **配置编辑页** (784×600): 配置管理 & 工具修复(2026-08-08 高度由 720 缩至 600,5/6)
- **修改头像页** (784×600): 本地选图裁剪后写入游戏头像(2026-08-23 新增, 主页头像点击进入)

---

## 色彩体系

### 主色调

| 用途 | 色值 | 说明 |
|------|------|------|
| 深色背景 | `#0c0e14` / `#0f1117` | 深蓝黑 |
| 卡片背景 | `#161922` / `#1e293b` | 深灰蓝 |
| 侧栏背景 | `#11141d` | 深蓝 |

### 强调色(纯色,无渐变)

| 用途 | 色值 | 说明 |
|------|------|------|
| **主色(纯色)** | `#6495ED` | 矢车菊蓝:启动按钮、保存/运行按钮、激活导航、链接、输入 focus、版本徽章、编码激活 |
| 主色 hover(加深) | `#4F7FE0` | 按钮悬停态(纯色加深,替代渐变提亮);启动中态同色 |
| 主色浅(文字/高亮) | `#9DB9F3` | 头像首字、版本徽章字、chip 已添加态文字、类别标签字 |
| 主色柔(图标/辅助) | `#8FB1F0` | 导航激活图标/文字 |
| 蓝色(兼容) | `#3b82f6` / `#60a5fa` | 已并入 #6495ED 体系,不再单独使用 |

### 文字色

| 用途 | 色值 |
|------|------|
| 主文本 | `#ffffff` (100%) |
| 次文本 | `#e2e8f0` |
| 辅助文本 | `#a0aec0` / `#64748b` |
| 禁用文本 | `#6b7280` |
| 成功提示 | `#10b981` |
| 警告/运行中 | `#f59e0b` |
| 错误/高危 | `#ef4444` |

### 边框 / 分割线

| 用途 | 值 |
|------|-----|
| 细边框 | `rgba(255, 255, 255, 0.05)` |
| 明显边框 | `rgba(255, 255, 255, 0.1)` |
| 主色边框(强调) | `rgba(100, 149, 237, 0.3)` |

> 完整令牌(含已收编的启动态色 launch-busy/launch-done/launch-glow 等)见 `tokens.md` §1。

---

## 字体系统

### 主字体

- **中文/英文/数字**: `HarmonyOS Sans SC`(鸿蒙字体,华为官方 **免费商用**;400/500/700/800 四字重,本地自包含 woff2 见 `docs/fonts/sc/`;回退 Microsoft YaHei)
- **代码/路径**: `JetBrains Mono` (400, 700),回退 Consolas

### 字号层级

| 用途 | 大小 | 粗细 | 示例 |
|------|------|------|------|
| 启动按钮图标 | 44px | — | 主页启动按钮(圆形纯图标) |
| 头像占位首字 | 36px | 700 | "汤" |
| 页面标题 | 20px | 700 | "常用设置"(编辑页分组头;v2.3.1 由 800 改 700 — 800/Black 字重未随包分发, 见坑位记录) |
| 主页大昵称 | 24px | 700 | "米汤"(主页,2026-08-30 HTML 对齐实机) |
| 导航图标 | 20px | — | 侧栏图标 |
| 昵称/主页标题 | 18px | 700 | 标题栏"汤圆启动器" |
| 启动命令输入框 | 16px | — | 加载器左栏 textarea(mono) |
| 在线人数/标签 | 15px | 600/400 | "12 人"、"在线"(mono,同基线) |
| 卡片标题/工具名 | 14px | 700 | "游戏昵称" |
| 输入框值 | 14px | 400 | 昵称输入 |
| 下拉框值 | 13px | 400 | 界面语言 |
| 描述文本 | 12px | 400 | "设置你在游戏内显示的名字" |
| 标签/提示 | 11px | 500 | 风险等级、chips、编码按钮 |
| 徽章/类别标签 | 10px | 400/700 | 版本号、工具类别 |

---

## 间距 & 布局

### 间距单位 (px)

- **最小**: 4px (微调)
- **小**: 8px / 10px (紧密相邻元素、卡片组 gap)
- **标准**: 12px / 14px / 16px (组内间距、卡片内边距)
- **大**: 20px / 24px (区域间距)
- **主页特例**: 36px 横向 / 48px 底部 (launcher-card padding)

### 圆角半径

> **v2.3.1 二轮用户决策 (2026-08-30): 推翻 2026-08-22 矩形化, 改 Win11 圆角档位**。
> 窗口外壳走 Win11 原生 DWM 圆角 (`DwmSetWindowAttribute`, 系统级渲染无黑边 — 当年黑边问题是 Flutter 自绘圆角路线的缺陷, DWM 路线不存在); 失败自动回退矩形。
> 完整令牌见 `tokens.md` §3。

| 用途 | 值 |
|------|-----|
| 窗口外壳 | DWM 系统圆角 (~8px, 失败回退矩形) |
| 卡片/面板/弹窗/菜单/悬停面板 | 8px |
| 按钮/输入框/下拉/chip/标签/窗口控制钮/导航项/提示条 | 4px |
| 胶囊(徽章/状态/开关) | 999px(保留) |
| 头像/启动按钮 | 50%(完美圆形,保留) |

### 窗口尺寸

| 项 | 值 |
|----|-----|
| 主页 | 360×510px (固定;v2.3.1 二轮回归 510 — 直连/练枪移出卡片, 悬停面板为覆盖层) |
| 编辑页 | **784×600px**(固定,2026-08-08 由 720 缩至 5/6) |
| 标题栏高 | 56px |
| 状态栏高 | 64px |
| 侧栏宽 | 96px(导航项高 80px) |
| 整体窗口边缘 | **矩形**(2026-08-22 用户决策: 放弃 16px 圆角 — Flutter Windows 圆角窗口四角黑边无法可靠解决, 改矩形彻底消除) |

---

## 组件库

> 完整组件清单(28 个组件:用途/props/何时用/何时不用/Flet 映射)见 **`design-system.md`**。以下为关键组件速查。

### 启动按钮 (LaunchButton,主页主操作)

- 尺寸: **110×110px 圆形**,纯图标(rocket,44px),**无文字**
- 背景: 纯色 `#6495ED`(矢车菊蓝,无渐变)
- 阴影: 无彩色发光,仅极淡中性投影 `0 2px 8px rgba(0,0,0,0.35)`
- hover: 纯色加深 `#4F7FE0` + 上移 -2px + 投影 `0 4px 12px rgba(0,0,0,0.4)`
- active: 缩小 scale(0.98)
- 状态机: 点击 → 启动中(`#4F7FE0`,1.5s)→ 已启动(`#10b981` + 绿辉光 `rgba(16,185,129,0.4)`,4s)→ 复位;期间防重复点击(title 同步:启动游戏/启动中…/游戏运行中)
- 过渡: `0.3s cubic-bezier(0.4, 0, 0.2, 1)`

### 配置入口(标题栏齿轮)

- 配置按钮已从主页卡片**删除**,改为标题栏窗口控制组最左的齿轮图标(win-btn,切换至编辑页)

### 服务器监控 (ServerMonitor,主页)

- 位置: 主页启动台,昵称下方、启动按钮上方
- 形态: 胶囊(半透明底 `rgba(255,255,255,0.04)` + 细边框,圆角 pill,padding 5px 12px)
- 内容: 状态圆点(8px) + 标签 + 人数;**标签与人数同字号 15px mono、同基线对齐**
- 状态圆点: 在线=`#10b981` 绿 + 3px 浅绿外晕;离线=`#64748b` 灰
- 文案: 在线「在线 N 人」;离线「离线」(隐藏人数)
- **⚠ 数据契约(用户红线)**: 人数必须来自真实查询,禁止展示编造数字。真实实现用 A2S 查询(数据源 = rev.ini `Loader.ConnectServer` 的 IP:端口);查询失败/未配置 → 灰点「离线」,不显示人数。HTML 预览中的「12 人」仅为 UI 形态演示(注释已标注)

### 配置卡片 (ConfigCard)

- 内边距 16px,**矩形** (2026-08-22 去圆角),背景 `#161922`,边框 `1px rgba(255,255,255,0.05)`
- 结构: 标题行(标题)+ 描述 + 输入控件 (v2.3.1 三轮: 键名标签已删, 高级场景 tooltip)
- hover: 背景 `rgba(255,255,255,0.03)` + 边框 `rgba(100,149,237,0.3)` + 右移 translateX(4px)(**双列内 hover 不右移**)
- 双列(field-grid)卡片: flex 纵向 + 控件 `margin-top:auto` 贴底对齐(描述行数不同不影响控件对齐)

### 输入框 (InputDark)

- 尺寸: 通栏高 40px,**矩形** (2026-08-22 去圆角),字号 14px
- 背景: `rgba(0, 0, 0, 0.2)`;边框: `1px rgba(255,255,255,0.1)`
- focus: 边框 `#6495ED` + `box-shadow: 0 0 0 2px rgba(100, 149, 237, 0.2)`
- 变体: mono(等宽,如 IP:端口);textarea(多行,加载器左栏 16px mono)

### 导航项 (SidebarNav)

- 尺寸: 侧栏宽 96px;导航项高 80px,**矩形** (2026-08-22 去圆角),竖向(图标在上 20px,文字在下 10px),gap 4px
- 激活态: 背景 `rgba(100, 149, 237, 0.1)` + 文字/图标 `#8FB1F0` + **左侧 3px 主色指示条 `#6495ED`**

### 工具卡片 (ToolCard,双栏网格)

- 布局: 2 列 × 2 行(4 张卡),gap 10px;执行超时设置行(输入框 110px 数字居中 + 说明文字)通栏置于卡片网格上方
- 卡片: padding 16px,**矩形** (2026-08-22 去圆角),背景 `#161922`,边框 `1px rgba(255,255,255,0.05)`;hover 边框 `rgba(100,149,237,0.3)`(无右移)
- 结构: 顶行(类别标签 + 风险标签 | 运行按钮)→ 工具名(14px W700)→ 状态文本
- **删除项**: 描述小字与「执行: xxx」动作行已删(用户不需要知道内部命令)
- 类别标签: 主色底 15% + `#9DB9F3` 字,10px,**矩形** (2026-08-22 去圆角)
- 风险分级色: 低=`#10b981` 绿 / 中=`#f59e0b` 琥珀 / 高=`#ef4444` 红
- 运行按钮: 主色实心,play 图标 + 「运行」;运行中: clock 图标 + 「运行中」 + disabled;完成绿 / 失败红状态文本

### 加载器分栏 (LaunchSplit)

- 启动命令卡: 左右分栏 **3:2**(左=手动输入 textarea 16px mono,右=推荐启动项面板)
- 右栏 RecPanel: 标题「推荐启动项」+ 竖排 chips(单行省略防溢出);chips 点击切换 ✓ 已添加态
- 左栏 textarea: 每参数一行,保存时自动合并

### 状态栏 (StatusBar,编辑页)

- 高度 64px,底部: 左=**状态图标(仅图标,check-circle 15px 绿,无文字无时间戳)**,右=编码切换(ANSI/UTF-8 分段)
- 「就绪 | 已成功加载配置文件」文字与时间戳**已删**(用户不需要)

---

## 图标系统

- **全部使用内联 SVG**(lucide 风格,`stroke=currentColor`,stroke-width 2,24×24 viewBox),**禁止 emoji**
- 图标库通过 `<symbol>` 定义 + `<use href="#i-xxx">` 引用,`svg.ic { width:1em; height:1em }` 跟随父级字号
- 现有图标: home / settings(齿轮)/ rocket / arrow-left / folder / folder-open / file / user / wrench / check-circle / play / clock / save / minus / x
- 图标颜色继承 `currentColor`,随所在元素文字色变化(如激活导航项变亮、运行中切换 clock)

---

## 动画 & 交互

> v2.3.1 三轮动效令牌化 (tokens.md §7): `motion-fast` 150ms / `motion-normal` 220ms / `ease-standard`; 只动 opacity/transform; 循环动画每页 ≤1。

| 项 | 值 |
|----|-----|
| 缓动 | `ease-standard` (全部动效唯一缓动) |
| 悬停效果 | 背景加深 + 上移 -2px + 阴影增强(主按钮); 背景微亮 + 边框高亮(卡片) |
| 视图切换 ① | 内容 220ms 淡入 + 上移 8px (view-in) |
| 分组切换 ⑥ | 内容 180ms 淡入 + 上移 6px (group-in) |
| 面板/菜单 ② | 150ms 下滑 + 淡入 (panel-in / panel-in-center) |
| 弹窗 ③ | 150ms scale 0.96→1 + 淡入, 遮罩 150ms 淡入 (dialog-in / mask-in) |
| 在线点 ④ | 2s 周期透明度呼吸 1↔0.55 (唯一循环动画) |
| 启动按钮 ⑤ | 按下 scale 0.97; 启动中火箭抖动 0.45s ×2 (有限, 非循环) |
| 主题换装 ⑦ | 重建视图 220ms 淡入, 避免闪变 |

### 滚动条 (自定义)

- 宽度 6px;轨道 `rgba(255,255,255,0.02)`;滑块 `rgba(255,255,255,0.1)` 圆角 3px;hover 滑块 `rgba(255,255,255,0.2)`

### 背景光晕 (装饰)

- **已移除**(用户决定:不需要发光效果)

---

## 标签体系

### 版本徽章

- 背景: `rgba(100, 149, 237, 0.2)`;文字: `#9DB9F3` W700 10px;边框: `1px rgba(100,149,237,0.3)`;圆角 pill;padding 8/2px

### ~~代码标签 (键名)~~(已删, v2.3.1 三轮)

- rev.ini 键名标签整体移除 (rules.md §4.3 隐藏技术细节红线的落实); 页头 kicker 同批删除。

---

## 状态指示

### 就绪状态(编辑页状态栏)

- 图标: check-circle(绿色 `#10b981`),**仅图标,无文字、无时间戳**

### 错误状态

- 红色图标/文字,3 秒自动消失(预留;当前 UI 无展示位)

---

## 网格系统

### 主页布局 (360×510)

```
┌─────────────────────────┐
│ H:56px - 标题栏          │
│   (版本徽章 + 产品名15px  │
│  练枪/主题/配置/─/×)     │
├─────────────────────────┤
│  W:100×H:100 - 头像     │
│  昵称(24px)              │
│  状态胶囊(主服+chevron)  │
│  W:110×H:110 - 启动按钮  │
│  [悬停胶囊 → ServerPanel │
│   分服面板(覆盖层)]      │
└─────────────────────────┘
```

### 编辑页布局 (784×600)

```
┌────────────────────────────────┐
│ H:56px - 头部 (返回/打开cfg/    │
│   指定目录/打开文件/保存 + 窗口) │
├──────────────┬─────────────────┤
│ W:96px       │                 │
│ 竖向导航     │ 主内容区 (卡片)  │
│ (图标上下)   │   (滚动)         │
├──────────────┴─────────────────┤
│ H:64px - 底部状态栏 (仅图标+编码)│
└────────────────────────────────┘
```

---

## 可访问性 & 最佳实践

1. **选区禁用**: `user-select: none` (标题、头像、徽章、标签、状态栏上)
2. **指针事件**: 装饰元素 `pointer-events: none`(当前无装饰光晕)
3. **边框样式**: 使用 `1px rgba()` 而非实心色,融合深色背景
4. **对比度**: 所有文字与背景对比度 >= 4.5:1
5. **省略策略**: 长文本(昵称/启动项标签)单行省略,不换行撑破布局

---

## 坑位记录(实现阶段必读)

> 2026-08-23 补充: **CFG 配置页** — 编辑页导航第 4 项(图标: 文档), 表单化编辑 s0up 预设(auto.cfg + crosshair.cfg):
> 4 组 23 字段(鼠标 6 / 准星 9 / 声音 4 / 性能 4); 数值范围校验标红不保存; 保存前 .bak 备份;
> 独立 dirty(返回/切页/关窗确认); 准星透明度 0-100% 显示(文件值 0-255 换算);
> 数字字段等宽字体 + 全部控件 64px 高 + desc 18px 行 → 双列卡片等高 (v2.3.1 二轮修正: 控件 64→48px、字 15/13px, 64 框小字头重脚轻);
> 生效机制 = ProcName 启动参数 `+exec auto.cfg`(最后执行覆盖 config.cfg, 新装默认含, 已装一键添加);
> 缺失预设文件 → 红色提示条 + 一键重新植入(源 = assets/s0up_preset/);
> 引擎 = `app/cfg_fields.py`(解析/写回保留注释/对齐/引号/CRLF, 缺失命令追加组标题节)。
>
> 2026-08-23 补充: **修改头像页** — 主页头像点击进入(784×600):
> 本地选图(jpg/png/webp/bmp, 限 20MB/8000×8000) → 1:1 裁剪框(拖动位置 / 拖四角缩放 / 滚轮缩放, 32px~画布边长);
> 保存 = PIL 裁剪 → 缩放 64×64 → 转 24 位 BMP 写 `platform/avatar.dat` + PNG 写 `platform/avatar1.dat`;
> 替换前 .bak 备份(仅保留最近一个) + 「恢复默认头像」(出厂默认随包备份 assets/);
> 透明区域填黑(24 位 BMP 无 alpha 通道); 重启游戏生效(游戏运行中不会覆盖, 已实测);
> 保存后主页头像立即刷新; 裁剪映射(屏幕坐标→原图像素)按 cover 缩放反推, 见 HTML `applyPreviewTo`。

| 坑 | 说明 |
|----|------|
| 天蓝阴影渲染成橄榄线 | 旧主题 #87CEEB 阴影在深底上发绿——已弃用 |
| 字重 800 未随包 | 设计曾用 800(Black) 渲染页面标题, 安装包只带 400/500/700 → 陌生机器回退。v2.3.1 定稿: **设计字重限 400/500/700**(rules.md §4.5) |
| HTML 圆角同步滞后 | 2026-08-22 矩形化后 HTML 一直未同步(v2.3.1 一轮补齐), 二轮又改回 Win11 圆角 — 教训: **形状决策变更必须当轮同步 HTML+三件套** |
| "web coding 味" | 外部反馈 UI 像网页开发者面板 — 根因: 代码注释装饰(kicker)/键名标签/mono 滥用/web 式 hover 位移。v2.3.1 三轮全数移除; 教训: **面向玩家的 UI 不引入任何"源代码审美"元素** |
| 标题栏 5 钮挤换行 | 主页标题栏 5 个 win-btn(练枪/主题/配置/─/×)后, 产品名 18px 会换行 — 定稿 15px + nowrap |
| SCALE 过渡 0.9 缩放残留 | 视图切换避免 scale 动画残留 |
| Dropdown 被圆角窗口裁切 | ~~16px 圆角窗口~~已改矩形(2026-08-22), 圆角裁切风险消除; frameless 弹出层仍可能被窗口边缘裁切, 需实测 |
| 主页不放状态栏 | 主页 360 宽无状态栏空间,状态信息只放编辑页 |
| select 与 input 同高 | appearance:none 的 select 在 Chrome 有 baseline 偏移,需 display:block + 等高 40px |
| chips 溢出 | 竖排 chips 需 `min-width:0; width:100%; nowrap; ellipsis`,容器 overflow hidden |
| grid 比例被内容撑破 | 分栏用 `3fr minmax(0,2fr)` 防 min-content 撑宽 |
| 离线字体度量 ≠ 实机 | HTML 字体度量与 Flutter 渲染有差异,最终以 Flet 实机为准 |
| **flet 0.86.5 offset 位移动画毁布局** (v2.3.1 实装) | `offset`/`animate_offset` 用在 ListView 内卡片或 Column 子控件上,实机反复出现 内容区空白(元素在树里不绘制)/控件叠错位,多轮复现 — view-in/group-in/panel-in/hover 位移全数降级为即时切换,保留纯 opacity/rotate/scale;**flet 升级前勿再试 offset 类动效** |
| **IconButton 默认 40×40** (v2.3.1 实装) | 主页标题栏 5 个 win_btn 撑出 360px 窗宽,「关闭」被裁掉 — IconButton 必须显式 `width=28, height=28` |
| **page.theme_mode 必须随深浅换装** (v2.3.1 实装) | 无显式颜色的 Text 按 theme_mode 取默认色;set_scheme 换 theme.* 而 theme_mode 卡在 DARK → 浅色下一族标题/说明白字不可读 |
| **on_hover 事件 data 是真布尔** (2.3.0 存量, v2.3.1 修) | flet 0.86.5 `e.data` 为 bool,按字符串 `"true"` 比较使全部 hover 处理器恒走 else 分支 — 统一 `_is_hovered()` 兼容 |
| **page.open() 不存在** (2.3.0 存量, v2.3.1 修) | flet 0.86.5 弹对话框用 `page.show_dialog()` / `page.pop_dialog()` |
| **theme.COL_BG/COL_CARD 别名须是模块常量** (2.3.0 存量, v2.3.1 修) | 历史别名只写进 `_LIGHT` 覆盖表时,`_DARK` 快照不含它,切回深色残留浅色值(根容器/导航白底白字);夜间定时启动首帧即崩 |
| **Control 子类禁用 `_values`/`_dirty`/`_frozen` 实例属性名** (2026-08-30 审查) | flet 基类把它们用作响应式存储,`super().__init__()` 会覆盖子类同名属性 — EncGroup 的 `_values` 被换成 dict,`selected` setter 的成员判断静默失真(赋值无声变 no-op)。自定义私有属性加业务前缀(如 `_enc_values`) |
| **`ft.WindowEventType` 是普通 Enum** (2026-08-30 审查) | `e.type` 是枚举成员,`== "blur"` 字符串比较恒 False;经 `.value` 取值比较(并用 `getattr(t, "value", t)` 兼容裸字符串载荷) |

---

## 交付文件

- ✅ 主页启动台 HTML (完整功能演示)
- ✅ 配置编辑页 HTML (完整功能演示)
- ✅ 组件库文档 (`design-system.md`)
- ✅ 令牌字典 (`tokens.md`)
- ✅ 编码约束 (`rules.md`)

---

## Flet 翻译注意点(实现阶段必读)

| 规范项 | Flet 落地要点 |
|--------|---------------|
| HarmonyOS Sans SC / JetBrains Mono | woff2 存 `docs/fonts/sc/`(400/500/700/800)。Flet 版需回退 Microsoft YaHei 或随包打包字体文件 |
| **纯色主按钮 #6495ED** | `ft.Container` + `bgcolor=COL_BRAND`(无渐变);hover 加深 `COL_BRAND_HOVER`(on_hover 切 bgcolor) |
| 启动按钮 110px 圆形纯图标 | `ft.Container`(110×110, border_radius=55)+ 居中 `ft.Icon(rocket_launch, 44, white)`;状态机 busy/done 走 `COL_BRAND_HOVER`/`COL_OK` + `launch-glow` 阴影 |
| 无发光 | 启动按钮仅极淡中性投影;头像无外发光;无背景光晕(blur 性能差且用户否决) |
| 图标系统 | Flet 用 `ft.Icons`(Material 图标,与 lucide 近似语义): home/settings/rocket_launch/arrow_back/folder/folder_open/description/person/construction/check_circle/play_arrow/hourglass_top/save/remove/close;无 emoji |
| 按钮上移 -2px | Flet Container 支持 `offset` 动画或 margin 切换;或降级为仅提亮(需实机验证) |
| 卡片右移 translateX(4px) | Flet 无 translateX 直接等价物 → Container offset 动画;若卡顿降级为仅边框高亮 |
| 自定义滚动条 | Flutter 默认滚动条样式有限,自定义 6px 细滚动条需 `ScrollbarTheme` 或降级默认 |
| 左侧 3px 指示条 | 导航项 Container 左边框 3px solid #6495ED + 激活背景 |
| 窗口矩形边缘 | frameless + 深色背景;**矩形**(2026-08-22 用户决策, 放弃 16px 圆角 — Flutter Windows 圆角窗口四角黑边问题, 详见 DESIGN.md 坑位) |
| 头部 56px / 状态栏 64px / 导航 96px | 替换现有 64px 标题栏 / 40px 状态栏 / 88px 导航;**编辑页窗口 784×600px**;顶栏右侧目录信息/rev.ini 徽章已删;保存按钮在顶栏左侧;状态栏仅图标+编码切换 |
| user-select none | Flet 默认文本不可选,无需额外处理 |
| 字号体系 | 44/36/20/18/16/15/14/13/12/11/10px → Flet Text size 一一对应 |
| 主色统一 | 所有主操作/激活态统一 `#6495ED`(含白字按钮,深底上对比 ≥4.5:1) |
| 服务器监控 | 主页 server-monitor: 绿点+人数胶囊。Flet 实现: 读 `Loader.ConnectServer` → A2S_INFO/A2S_PLAYER 查询 → 在线=绿点+N人 / 失败=灰点「离线」**且隐藏人数**。**禁止编造人数**(用户红线);定时轮询(如 30s) |
| Win11 圆角 (v2.3.1 二轮) | 窗口: frameless 下 ctypes 调 `DwmSetWindowAttribute(hwnd, 33, 2, 4)`(DWMWCP_ROUND), 按 PID/标题找 HWND; 失败回退矩形并 set_status 留档。控件: Container/按钮 `border_radius` 走 `RADIUS_CARD`(8)/`RADIUS_CTRL`(4) |
| 服务器悬停面板 (v2.3.1 二轮) | 主页 ServerPanel: `ft.Stack` 覆盖层锚定胶囊下方(w=264 圆角8); 悬停胶囊 `on_hover` 展开/移出 150ms 收起(Flet 用延迟 Timer); 行悬停显示「进入」`ft.IconButton`(28×28 圆角4, 箭头); 数据 = 状态 API + 预设 sid 映射; 「进入」= 一次性 +connect 启动不改 ConnectServer |
| 练枪入标题栏 (v2.3.1 二轮) | 主页标题栏 WinBtn 练枪变体(准星图标, tooltip 练枪启动) = `on_launch_click(practice=True)`; 主页卡片四件套回归(头像/昵称/胶囊/启动钮); 直连下拉与自定义服务器弹窗删除 |
| 服务器监控 | 主页 server-monitor 胶囊: 只显示**主服**(ConnectServer 目标)单服状态(Q4=B); 数据源 = cs.suchitems.top/api/status + 预设 sid 映射; **禁止编造人数**(用户红线); 30s 轮询 |
| 主题切换 (v2.3.1 收编) | WinBtn 变体(月亮/太阳随当前方案) + ThemeMenu 菜单(深/浅/定时 ✓ 态 + 深色时段设置…);主页+编辑页标题栏, 修改头像页不放(裁剪中换装丢进度);浅色令牌表见 tokens.md §1.8, Flet 侧动态访问 `theme.COL_X`(from-import 冻结深色值) |
| 自定义下拉 (v2.3.1 四轮) | Flet `ui.select_dark` 重构为自绘: 收起=InputDark 同款按钮, 展开=Stack 菜单面板 (卡片底/圆角8/柔影, 选中 brand 底), 箭头旋转+panel-in 150ms; 原生 ft.Dropdown 弹出层弃用 |
| 动效映射 (v2.3.1 三轮) | Flet: 视图/换装淡入 = AnimatedOpacity+offset (220ms); 面板/弹窗 = AnimatedOpacity + offset(-4px)/scale 0.96 (150ms); 在线点呼吸 = Opacity 循环控制器或 AnimationTicker (唯一循环); 火箭抖动 = rotate/offset 有限次; 全部失败降级即时切换 (rules §4.6) |
| 表单弹窗 (v2.3.1 收编) | FormDialog 统一形态: AlertDialog(bg `bg-card`, 矩形) + 标题 15/700 + 行 + desc 11px + 取消(TextButton)/保存(FilledButton 主色实心);现役: 深色时段、自定义服务器 |
| 提示条 (v2.3.1 收编) | CFG 页 HintBar: warn/err 两个变体(半透明警示底 + 同色边框 + 图标 + 文案 + 主色实心动作按钮), 条件显示 |
| 字号字重 | Flet 页面标题/分组标题一律 W_700(W_800 已弃, 随包无 800 字重);主页昵称 size 24 |
| 去 web coding 味 (v2.3.1 三轮) | 删: 页头 kicker、设置卡键名标签 (CodeTag), hover 位移取消;mono 收窄至 启动命令/启动参数/IP:端口/CFG 数值;版本徽章/在线人数改 font-cn + tabular-nums;卡片加 `SHADOW_CARD` 柔影 (token `card-shadow`);主页内容区底层加品牌氛围垫层 (token `home-wash`, radial 椭圆, 非发光) |
| 组件化 | 全部自绘组件落 `flet_app/components/ui/`,颜色走 `flet_app/theme.py` 令牌(见 `rules.md`) |
